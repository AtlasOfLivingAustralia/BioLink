<UserControl x:Class="BioLink.Client.Taxa.TaxonExplorer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:src="clr-namespace:BioLink.Data;assembly=BioLink.Data"
             xmlns:data="clr-namespace:BioLink.Data;assembly=BioLink.Data"
             
             mc:Ignorable="d" 
             d:DesignHeight="376" d:DesignWidth="225">
    <Grid>
        <TreeView Name="treeView">
            <TreeView.Resources>
                <!--ItemsSource="{Binding Path=Children}" -->
                <HierarchicalDataTemplate DataType="{x:Type data:Taxon}" >
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding Path=TaxaFullName}"/>
                    </StackPanel>
                </HierarchicalDataTemplate>
                <!--
                <Style TargetType="TreeViewItem">
                    <Setter Property="IsExpanded" Value="{Binding Path=IsExpanded, Mode=TwoWay}" />
                    <Setter Property="IsSelected" Value="{Binding Path=IsSelected, Mode=TwoWay}" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsChanged}" Value="True">
                            <Setter Property="Background" Value="Red" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
                -->
            </TreeView.Resources>
            <TreeViewItem>
                <TreeViewItem.Header>Loading...</TreeViewItem.Header>
            </TreeViewItem>
        </TreeView>
    </Grid>
</UserControl>

    