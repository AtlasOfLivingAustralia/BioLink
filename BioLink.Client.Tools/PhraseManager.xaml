<Window x:Class="BioLink.Client.Tools.PhraseManager"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="384" d:DesignWidth="542" Title="Phrases">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="23" />
            <RowDefinition Height="*" />
            <RowDefinition Height="40" />
        </Grid.RowDefinitions>
        <Menu Grid.Row="0" />
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />                
            </Grid.ColumnDefinitions>
            <Grid x:Name="lhsGrid">
                <Grid.RowDefinitions>
                    <RowDefinition Height="34" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="30" />
                </Grid.RowDefinitions>
                <Grid Grid.Row="0">
                    <Label Content="Filter:" Margin="10,4,0,0" Height="23" VerticalAlignment="Top" HorizontalAlignment="Left" Width="53" />
                    <TextBox x:Name="txtFilter" Margin="0,6,6,0" Height="23" VerticalAlignment="Top" HorizontalAlignment="Right" Width="185" />
                </Grid>
                <ListView Grid.Row="1">
                    <ListView.View>
                        <GridView>
                            <GridView.Columns>
                                <GridViewColumn Header="Phrase Category" Width="{Binding ElementName=lhsGrid, Path=Width}" />
                            </GridView.Columns>
                        </GridView>
                    </ListView.View>
                </ListView>
                <Button Content="..." Grid.Row="2" Height="24" HorizontalAlignment="Left" Margin="6,2,0,0" Name="button4" VerticalAlignment="Top" Width="24" />
            </Grid>
            <GridSplitter />
            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="34" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <ToolBar Grid.Row="0">
                    <Button x:Name="btnAddPhrase" Content="+" Height="24" Width="24"/>
                    <Button x:Name="btnDeletePhrase" Content="-" Height="24" Width="24"/>
                    <Button x:Name="btnRenamePhrase" Content="R" Height="24" Width="24"/>
                </ToolBar>
                <ListView Grid.Row="1" />
            </Grid>
            
            
        </Grid>
        <Grid Grid.Row="2">
            <Button Content="_Cancel" Height="23" HorizontalAlignment="Right" Margin="0,0,8,8" Name="btnCancel" VerticalAlignment="Bottom" Width="75" IsCancel="True" />
            <Button Content="_Apply" Height="23" HorizontalAlignment="Right" Margin="0,0,89,8" Name="btnApply" VerticalAlignment="Bottom" Width="75" IsEnabled="False" />
            <Button Content="OK" Height="23" HorizontalAlignment="Right" Margin="0,0,170,8" Name="btnOK" VerticalAlignment="Bottom" Width="75" IsDefault="True" />
        </Grid>
    </Grid>
</Window>
