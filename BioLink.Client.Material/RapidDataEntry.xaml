<ext:DatabaseActionControl x:Class="BioLink.Client.Material.RapidDataEntry"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ext="clr-namespace:BioLink.Client.Extensibility;assembly=BioLink.Client.Extensibility"
             xmlns:mat="clr-namespace:BioLink.Client.Material"
             mc:Ignorable="d" 
             d:DesignHeight="550" d:DesignWidth="826" >
    <Grid>
        <Grid.Resources>
            <ext:NegatingConverter x:Key="NegatingConverter" />            
        </Grid.Resources>
        <ScrollViewer>
            <Grid MinWidth="600" MinHeight="450">
                <ext:ItemsGroupBox Margin="6,6,6,0" Name="grpSites" VerticalAlignment="Top" Height="170" HeaderPrefix="Site:" Header="{Binding SiteName}" HeaderFontWeight="Bold" IsUnlocked="{Binding Locked, Converter={StaticResource NegatingConverter}}" HeaderForeground="{Binding HeaderForeground}" AddNewClicked="grpSites_AddNewClicked">
                </ext:ItemsGroupBox>
                <ext:ItemsGroupBox Height="62" Margin="6,182,6,0" Name="grpSiteVisits" HeaderPrefix="Visit:" Header="{Binding SiteVisitName}" VerticalAlignment="Top" IsUnlocked="{Binding Locked, Converter={StaticResource NegatingConverter}}" HeaderForeground="{Binding HeaderForeground}" AddNewClicked="grpSites_AddNewClicked" />
            </Grid>
        </ScrollViewer>
            
    </Grid>
</ext:DatabaseActionControl>
