<UserControl x:Class="BioLinkApplication.LoginControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="350" d:DesignWidth="650">
    <UserControl.Resources>
        <Style TargetType="Button">
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect />
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
        
    <Grid Height="350" Width="650" SnapsToDevicePixels="True">        
        <Grid.RowDefinitions>
            <RowDefinition Height="80" />
            <RowDefinition Height="150"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="400" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Label Content="{StaticResource LoginControl.lblUsername}" Grid.Column="1" Height="28" HorizontalAlignment="Left" Name="lblUsername" VerticalAlignment="Top" HorizontalContentAlignment="Right" Width="152" Grid.Row="1" Margin="0,2,0,0" />
        <Label Content="{StaticResource LoginControl.lblPassword}" Height="28" HorizontalAlignment="Left" Margin="0,31,0,0" Name="lblPassword" VerticalAlignment="Top" Grid.Row="1" Width="152" HorizontalContentAlignment="Right" Grid.Column="1" />
        <TextBox Grid.Column="1" Grid.Row="1" Height="23" HorizontalAlignment="Left" Margin="158,2,0,0" Name="txtUsername" VerticalAlignment="Top" Width="120" />
        <PasswordBox Grid.Column="1" Grid.Row="1" Height="23" HorizontalAlignment="Left" Margin="158,31,0,0" Name="txtPassword" VerticalAlignment="Top" Width="120" />
        <Button Content="{StaticResource LoginControl.btnLogin}" Grid.Column="1" Grid.Row="1" Height="23" Margin="170,68,160,59" Name="btnLogin" Width="70" Click="btnLogin_Click" SnapsToDevicePixels="True" />
    </Grid>
</UserControl>
